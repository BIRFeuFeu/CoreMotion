/* =========================================================
   CONFIGURAÇÃO LOCAL / STAGING — modelo
   ---------------------------------------------------------
   Copie este arquivo para `config.local.js` (que está no
   .gitignore e portanto nunca é commitado) e aponte para o
   seu projeto Supabase de testes:

       cp config.local.example.js config.local.js

   O `config.local.js` vence o `config.js`. Se você não criar
   o arquivo, o app usa o `config.js` normalmente.
   ========================================================= */
window.COREMOTION_CONFIG_LOCAL = {
  url: "https://SEU-PROJETO-DE-TESTE.supabase.co",
  anonKey: "COLE-AQUI-A-ANON-KEY-DO-PROJETO-DE-TESTE",
};
